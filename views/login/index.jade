extends ../layout

block content
  div(class="span7")
    legend
      h1= locale.header
    include ../flashify
    form(action="/login", method="post")
      input(type="text",name="username",value="",placeholder="#{locale.username}")
      br
      input(type="password",name="password",value="",placeholder="#{locale.password}")
      br
      input(type="submit",name="cmdLogin",value="#{locale.button}",class="btn btn-primary")